export default class User {
  constructor(
    public name: string,
    public lastName: string,
    public city: string,
    public street: string,
    public number: number,
    public zip: number,
    public email: string
  ) {}
}
